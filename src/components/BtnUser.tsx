import { googleLogout, useGoogleLogin } from '@react-oauth/google';
import axios from 'axios';
import { useEffect } from 'react';

export default function BtnUser(props: any) {
    useEffect(() => {
        // axios.get()
    })
}